{
  "name": "gh-actions",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "husky install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^8.6.0",
    "husky": "^7.0.0",
    "lint-staged": "^12.1.7"
  },
  "lint-staged": {
    "**/*.{js}": [
      "npx eslint --fix tests --ext js"
    ]
  }
}
